{% form_theme form 'P5indicatoriUserBundle:Default:form_div_layout.html.twig' %}
<form id="projectNamesForm" class="form-horizontal login-form text-center" action="{{path("p5indicatori_query_source")}}" {{ form_enctype(form) }} method="POST">
                            {{ form_row(form.createdAtBefore) }}
                            {{ form_rest(form) }}
        <div class="control-group go-in-center" >
            <input class="btn" type="submit" name="submit" value="{{ 'Search'|trans }}" />
        </div>
    </form>
</div>
